# ===================================
# FAMO TrafficApp 3.0 - Environment Configuration
# ===================================
# 
# WICHTIG: Kopiere diese Datei nach .env.local und fülle die Werte aus!
# .env.local wird NICHT in Git committed (siehe .gitignore)
#
# Für Production: Nutze Secrets-Manager (AWS Secrets Manager, Azure Key Vault)

# OpenAI API Configuration
# Hole deinen Key von: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-DEIN_KEY_HIER

# LLM Configuration
LLM_MODEL=gpt-4o
LLM_MAX_TOKENS=1000
LLM_TEMPERATURE=0.3

# Application Configuration
APP_ENV=dev  # dev / staging / production
DATABASE_URL=sqlite:///data/traffic.db
ORIG_DIR=./tourplaene
STAGING_DIR=./data/staging
OUTPUT_DIR=./data/output
BACKUP_DIR=./routen

# OSRM Configuration
# Dev: Docker Desktop auf localhost
OSRM_BASE_URL=http://127.0.0.1:5000
OSRM_PROFILE=driving
OSRM_TIMEOUT=20

# Optional: Production OSRM
# OSRM_BASE_URL=http://172.16.1.191:5011

# Server Configuration
SERVER_PORT=8111
SERVER_HOST=0.0.0.0

# Logging
LOG_LEVEL=INFO  # DEBUG / INFO / WARNING / ERROR
